import 'package:get/get.dart';

/// Nothing feature-specific to register — the session controller is already
/// permanent from [AppBinding]. Present for symmetry and future use.
class SplashBinding extends Bindings {
  @override
  void dependencies() {}
}
